-> For the file sharing

-project name [enter on send]
-user name (your name) [enter on send]
-location [enter on send]
-with each record send:
    -top, length, bottom, species/density, weight, volume, time [saved automatically]

-let them choose to download it as a text file with formatting, email it, or text it to another person.

data should be saved every time the calculation runs. 
make it so the list items can be deleted.


Overview:
User enters data. 
Data shows up on record page. 
user can select items they want to share or delete them.
Then user selects how they want to send the data.
User enters the information and hits send.
